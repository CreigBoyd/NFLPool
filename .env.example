# Server
PORT=3001
NODE_ENV=production
CLIENT_URL=https://YOUR_GITHUB_USER.github.io/YOUR_REPO

# Database (MySQL)
DB_HOST=your_db_host
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=nfl_pool

# JWT
JWT_SECRET=replace_with_a_secure_secret
REFRESH_SECRET=

# SMTP (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_pass
SMTP_FROM="NFL Pool <no-reply@example.com>"

# Admin notification
ADMIN_EMAIL=admin@example.com